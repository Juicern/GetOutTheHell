<view class="van-circle">
    <canvas canvasId="van-circle" class="van-circle__canvas" style="width: {{utils.addUnit(size)}};height:{{utils.addUnit(size)}}"></canvas>
    <view class="van-circle__text" wx:if="{{!text}}">
        <slot></slot>
    </view>
    <cover-view class="van-circle__text" wx:else>{{text}}</cover-view>
</view>
<wxs module="utils" src="../wxs/utils.wxs" />